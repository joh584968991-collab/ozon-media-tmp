## 00:24 重要失误记录（用户提醒）

**错在哪**：我说"131 个 FAIL 在 xlsx 找不到图 → 建议删除"。

**为什么错**：我用了错误的"源数据假设"。ozon-lister 的批量图源是：
- `low62.txt`（待补分 SKU 清单）
- `62sku_info.json`（Ozon 后台 ir-20 原图 URL）
- `ozon_compliant_rework.py` 生成的 `_ozon_<i>.jpg` 二创版
- 最终 `batch_video62_v2.py` 用 jsDelivr 拉 `_ozon_*.jpg` 做视频

**我的查询错误**：用 `get_images_from_xlsx()` 查 xlsx，**但 xlsx 不是源**——所以即使 _ozon_*.jpg 在 jsDelivr 上 200 OK，我也判成"无源图"。

**关键教训**：

> **判断"能否修复"前，先追源数据链路。问 4 个问题：**
> 1. 这条流水线从哪里取输入？（xlsx? ir-20? jsDelivr? 用户输入?）
> 2. 中间是否有别的脚本二创？（_ozon_*.jpg 这种命名往往是二创产物）
> 3. 修复需要的是**原始图**还是**二创图**？
> 4. 失败视频的源数据是不是**已经准备好**？

**这次答案**：
- A) xlsx 有原图 → 13 个 → `ozon_video_pipeline.py` 直接重跑
- B) 已有 _ozon_*.jpg 二创图（≥4 张）→ 129 个 → 直接做视频步骤
- C) 62sku_info.json 有原图但未二创 → 2 个 → 先 `ozon_compliant_rework.py` 再做视频
- 真正无源 → 1 个（QK1321_hq 文件名带 _hq 后缀，估计 ozon-lister 误标）
- **总可修复：144/144**

**通用方法论**：

```python
# 不要假设数据源，先 audit 数据流
for source in [xlsx, info_json, cdn_jsdelivr, local_files, ali2ozon_pipeline]:
    count = len(items_in_source)
    print(f'{source}: {count}')
# 找覆盖率最大的那个，再决定 fix 路径
```

---

## 00:24 启动修复（老板指示"记住失误 开始修复"）

行动清单：
1. ✅ 写 memory（这一步）
2. ⏳ 跑 path A（13 个 ozon_video_pipeline.py）
3. ⏳ 跑 path B（129 个直接做视频）
4. ⏳ 跑 path C（2 个先二创再做视频）
5. ⏳ 处理 QK1321_hq（1 个单独查）
6. ⏳ 全部完成后全量自查（应该 283 PASS）
7. ⏳ push 到 git/jsDelivr

<!-- project: github.com/joh584968991-collab/ozon-media-tmp -->
