# 当前任务进度

## 任务卡片
- 任务名称：组合 SKU 的可验证单品来源回退
- 用户请求：有些组合 SKU 可以通过单品 SKU 反推出来。
- 成功标准：仅让组合采购价表明确且唯一的单品组成关系回退图片来源；每次生成回执记录目标组合和实际来源单品；其他情形继续阻断。
- 当前状态：已完成。

## 执行情况
- 已完成：读取组合采购价表；解析出 245 个有效组合关系；实现唯一单品来源回退和来源元数据；更新 Agent/skill 约束；完成正反样例及不推送端到端验证。
- 进行中：无。
- 下一步：仅在用户提供组合专属图片或明确确认合成素材策略时，单列支持多单品组合。

## 验证情况
- 已验证：组合采购价表结构为 245 个有效组合；17 个安全候选均只有一个组成单品且该单品在 xlsx 存在；`Y0005 → QK0023 × 2` 在 `--no-push` 下生成 `carousel_Y0005.mp4`，16 秒 / Main / yuv420p / tv；Y0028（多单品）和 Y9999（无映射）均以 FAILED 阻断。
- 未验证：真实 Git 推送/CDN 状态切换、真实 Ozon 写入（本轮范围明确不执行）。
- 验证结论：组合回退只发生在可审计的单一单品映射，不会放宽为标题、相似 SKU 或多单品猜测。

## 交接信息
- 关键文件：workbench/cost/组合采购价表.txt、AGENTS.md、ozon_video_pipeline.py、skills/ozon-carousel-video-pipeline/SKILL.md。
- 风险或阻塞：组合采购价表中的多单品组合不能安全地用任一单品图片代表；`combos.json` 只含成本，不含组成关系，不能单独作为映射来源。
- 需要用户确认：只有多单品组合的素材/表达策略需要额外确认。
